---
# host to test against
- hosts: test-kitchen
  remote_user: root

  roles:
    # name of role to test
    - role: ansible-mongodb
      mongodb_client: yes
      mongodb_server: yes
      mongodb_set_permissions: no
      mongodb_server_vesion: "3.2"

  vars:
    domain: test.com
    project: test-project
    environment_tier: development
